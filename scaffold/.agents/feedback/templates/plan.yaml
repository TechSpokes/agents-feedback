# Replace the example task with the accepted work and its proof of completion.
# yaml-language-server: $schema=../schemas/plan.schema.json
schema: feedback-plan.v1
owner: agent
tasks:
  - id: task-1
    task: "Describe one concrete task."
    status: planned
    validation: "Describe how to prove the task is complete."
# outcome: ""
